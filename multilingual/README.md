# Multilingual layer — Vanshita

Owns English/Hindi ARB resources, verified overrides, translation tooling, and ARB key-parity checks.

Every user-facing key added to `app_en.arb` must be added to `app_hi.arb` before merging.
