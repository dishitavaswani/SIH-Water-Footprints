# ML recognition — Kuhu

Owns food image recognition, `predict_label(image_path)`, confidence handling, and accuracy evaluation.

Large model binaries are deliberately ignored by Git. Store the model outside the repository or publish an approved download link and checksum here.
