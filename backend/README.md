# Backend — Aryaveer

Owns the FastAPI service, the `GET /footprint` and `POST /scan` endpoints, CORS configuration, and translation fallback.

Run from this directory after dependencies are installed:

```bash
uvicorn app.main:app --reload
```
